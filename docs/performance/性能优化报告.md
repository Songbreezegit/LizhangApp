# 性能优化报告（阶段验收）
本轮完成数据链路优化，保持界面设计、业务规则和 Room 表结构，未宣称整个性能专项完成。

## 已完成
- 首页使用年度数据库聚合与最近 8 条记录，减少全量数据读取。
- 日历使用月范围查询；统计单遍累加并在 Dispatchers.Default 执行。
- 查询增加 250 毫秒防抖，输入文字即时反馈。
- 线程内复用日期与金额格式器；日期跟随当前时区。
- 提醒关闭时不订阅全部礼金记录；日历列表使用稳定键。
- 撤回改变卡片外观的列表拆分，保持原设计。

## 构建兼容处理
2026-09-11，本机 JDK 默认临时目录的内部 Unix 域通信失败。给当前构建进程指定英文目录后恢复：
```powershell
$env:JAVA_TOOL_OPTIONS='-Djdk.net.unixdomain.tmpdir=D:\AndroidProjects\LizhangApp\build'
.\gradlew.bat test lint assembleDebug assembleRelease assembleDebugAndroidTest --console=plain
```
此为本机临时措施，未修改全局网络或共享 Gradle 配置。依赖解析需要允许网络访问。

## 验证结果
- test：Debug/Release 合计 122 项执行，0 失败、0 错误。
- lint：0 错误、14 警告。
- assembleDebug、assembleRelease、assembleDebugAndroidTest 全部成功。
- Pixel 9 / Android 17：RoomLedgerRepositoryInstrumentedTest 和 LargeDatasetPerformanceInstrumentedTest 共 4 项通过，使用独立内存数据库。
- Debug 安装启动成功；单次冷启动 1815 毫秒仅为冒烟数据，不作提速依据。
- Debug APK 21,125,778 字节；未签名 Release APK 14,020,951 字节，Release 未做安装验收。

## 单次合成数据探针
单位毫秒，联系人与记录各为指定数量，不包含用户数据。

| 数量 | 全量读取 | 搜索 | 联系人汇总 | 年度汇总 | 月范围 | 内存统计 |
| --- | --- | --- | --- | --- | --- | --- |
| 100 | 11.79 | 8.33 | 7.69 | 2.54 | 5.81 | 4.00 |
| 1000 | 38.40 | 35.77 | 51.43 | 4.45 | 6.92 | 12.49 |
| 5000 | 133.61 | 117.41 | 154.89 | 6.18 | 6.44 | 12.28 |

2026-07-29 历史单次内存统计为 43.87 / 28.44 / 331.06 毫秒。设备负载和 JIT 状态不同，部分未改动读取链路也变慢，不能据此计算提升比例。年度汇总和月范围查询工作量与全量读取不同。

## 待完成
同条件多轮冷／热启动、滚动帧率、保持卡片外观的惰性列表、真机主流程与长期内存观察。未启用发布包压缩或资源清理。当前分支 perf/app-performance-optimization，不自动合并 main。
