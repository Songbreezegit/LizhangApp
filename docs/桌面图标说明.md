# 桌面图标说明

2026-09-17：使用用户提供的猫咪抱红包原图替换桌面图标。

- 原图位于 drawable-nodpi/launcher_cat.png，普通与圆形入口共用前景。
- 前景保留 18% 安全边距，由桌面应用自适应蒙版。
- 保留奶白背景；移除旧单色声明，避免主题图标继续显示旧礼盒。本轮未提供定制单色图标。
- 不修改应用标识、签名、数据库和页面逻辑。

如桌面缓存旧图标，可移除快捷方式后重新添加，无需清除应用数据。

验证：test、lint、assembleDebug、assembleRelease 全部通过。本轮未完成模拟器桌面外观验收。
